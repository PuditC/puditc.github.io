{
  "name": "puditportfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Static portfolio published to https://puditc.github.io — everything that ships lives in site/.",
  "scripts": {
    "check": "node scripts/check-links.mjs",
    "predeploy": "npm run check",
    "deploy": "gh-pages -d site"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
